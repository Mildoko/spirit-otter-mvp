# 局域网内部测试版

双击 `启动局域网演示版.cmd`，窗口会显示一个形如 `http://192.168.x.x:3001` 的地址。同一局域网内的 2～3 名测试成员在浏览器打开该地址即可。

如果页面显示进入确认界面，请使用固定内部邀请码：`OTTER-LAN-2026`。

- 使用根目录 `.env.local` 中已经配置的 DeepSeek 模型。
- 每个浏览器拥有独立的临时会话，成员不会共享对话。
- 不需要数据库、邀请码、Caddy 或 HTTPS。
- 关闭启动窗口即停止服务；重启服务后全部演示数据清空。
- 仅供可信局域网内部测试，不要做公网映射或公开传播。

如果其他设备无法访问，请确认设备连接同一个局域网，并允许 Windows 防火墙访问 TCP 3001 端口。
