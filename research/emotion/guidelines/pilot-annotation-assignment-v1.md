# 情绪标注试标任务 v1

本文件是人工工作队列，不代表这些记录已经完成人工标注。

## 第一轮：20 条独立试标

标注员 A 与标注员 B 分别独立标注以下记录，不查看候选标签：

`EMO-CN-001` 至 `EMO-CN-020`。

完成后由仲裁员逐条处理分歧，并在数据卡记录：主标签一致性、多标签 Jaccard、VAD/控制感分档差异，以及需要修改的标签定义。

## 第二轮：10 条复标

手册修订后，两名标注员再次独立标注：

`EMO-CN-001`、`EMO-CN-003`、`EMO-CN-005`、`EMO-CN-007`、`EMO-CN-009`、`EMO-CN-011`、`EMO-CN-013`、`EMO-CN-015`、`EMO-CN-017`、`EMO-CN-019`。

复标结果单独保存，不覆盖第一轮结果。只有完成双审和仲裁后，记录的 `annotationStatus` 才可改为 `adjudicated`。
