# 实施状态（2026-08-14）

## 已实现

- G11–G18 工程基础：运行模式、演示包、五维状态与平滑、模型降级、完整环境预检、正式界面、安全回归、CI 与交付文档。
- G19：产品行为、角色、记忆和准入文案统一为自动双灵、全局同意下静默记忆、最长 30 天。
- G20：版本化 Core Soul、深汐、拾岸、Lore Registry、启动校验、分层 Prompt Composer、角色化本地降级。
- G21：统一平滑链路、自然语言边界、带滞后的自动双灵状态机、数据库迁移、完整/demo API 与前端旧模式控件删除。
- G22：结构化记忆 Schema、三张记忆表、证据与修订、确定性 Guard、模型候选提取、去重替代、完整事务写入和 DemoStore 内存实现。
- G23：召回硬过滤、无向量排序、预算、Prompt 注入、行动/回访上下文、成功后召回计数、导出/删除/30 天清理。
- G24：单一连续角色界面、demo/lab 角色诊断、自动记忆说明、场景过渡和行动独立授权。
- G25：角色/路由/记忆冻结测试、Postgres 集成用例、桌面/Pixel 7 demo E2E、真实 DeepSeek 对照与严格包体门槛。

G26 是第二里程碑，尚未纳入首个包体：用户记忆策略、列表、编辑、逐条/全部遗忘和设置界面。按产品门槛，扩大到外部参与者前必须完成。

## 当前机器实际执行结果

- Prisma Schema 校验与 Client 生成：通过。
- 类型检查：shared/server/web 全部通过。
- 单元、冻结安全集与内容验收：92 项通过；另有 8 项 Postgres 集成测试在隔离的 PostgreSQL 16.15 测试库中全部通过。
- 构建：shared/server/web 全部通过。
- demo E2E：Desktop Chrome 与 Pixel 7 各 1 项通过。
- full E2E：Desktop Chrome 与 Pixel 7 各 1 项通过，覆盖一次性邀请码、自然语言自动整理、行动确认、刷新恢复、高风险退场和永久删除。
- 真实 DeepSeek：12 项对照已联网执行，非安全样本使用 `cloud_model`，报告不含完整用户原文。
- 密钥：只存在被 Git 忽略的 `.env.local`，未进入源码、日志、测试报告或包体。

## 当前发布阻塞项

- 首个内部可测试包体的代码、数据库、演示/完整 E2E 与真实模型门槛已具备实际执行证据。
- 扩大到外部参与者前仍需完成 G26 用户记忆设置中心。
- 目标局域网的 Caddy、CA、Secure Cookie、网络隔离、加密备份恢复和五人预试属于现场任务，尚未执行。

因此当前结论是：**角色与记忆首版已经达到内部完整可测试包体门槛，但尚未达到外部研究发布门槛。** `package:testable` 会在缺少 Postgres、full E2E 或真实模型对照时失败，禁止把跳过项目包装成“完整通过”。
