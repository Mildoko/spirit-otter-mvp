# 外网受控体验版

## 启动前

在项目根目录的 `.env.local` 中配置以下服务端变量。密钥不会进入网页包或 Git：

```env
LLM_PROVIDER="deepseek"
LLM_BASE_URL="https://api.deepseek.com"
LLM_API_KEY="你的 DeepSeek API Key"
LLM_MODEL="deepseek-v4-flash"
```

## 启动

双击 `启动外网体验版.cmd`。首次启动会从 Cloudflare 官方 GitHub Release 下载临时隧道工具，然后依次：

1. 构建当前网页与服务端；
2. 使用不含用户数据的中性样本探测 DeepSeek；
3. 探测成功后启动服务并开放临时 HTTPS 地址。

密钥缺失、模型不是 `deepseek-v4-flash` / `deepseek-v4-pro`、API 地址不是 DeepSeek 官方 HTTPS 地址，或者账户余额/网络导致探测失败时，程序会停止，不会用规则回复冒充真实模型。

窗口会显示：

- 一个 `https://*.trycloudflare.com` 外网地址；
- 一个每次启动随机生成的体验码。

将两者分别发给受邀朋友。朋友打开地址，逐项确认使用边界并输入体验码后即可进入。

默认最多授权 20 个浏览器会话。如需更少人数，可在项目根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-external-preview.ps1 -MaxSessions 8
```

## 横屏要求

- 欢迎页、内圈和外圈都只提供横屏版本。
- 竖屏时页面会被横屏门禁覆盖，不能跳过。
- 点击“全屏并尝试横屏”后，支持屏幕锁定的浏览器会自动切换；不支持时需开启系统自动旋转并手动横置设备。
- 用户仍可随时关闭网页或浏览器退出体验。

## 数据和准入边界

- 服务只监听本机 `127.0.0.1`，通过 Cloudflare 临时 HTTPS 随道转发。
- 未输入正确体验码时，服务端不允许读取或写入体验会话。
- 默认最多授权 20 个浏览器会话，各会话相互隔离。
- 对话和记忆只存在于本机进程内存中；关闭启动窗口后全部清空。
- 对话会通过服务端发送给 `.env.local` 中配置的 DeepSeek 官方 API；API Key 不会发送到浏览器。
- 每次启动前都会探测 DeepSeek 模型连通性，失败时不开放体验地址。
- 临时地址和体验码只能发给受邀成年朋友，不得公开发布或开放注册。

## 停止

在启动窗口按 `Ctrl+C` 或直接关闭窗口。临时地址会立即失效，服务端内存数据不会保留。
